# FRP Manager 下一阶段规划（v1.7+）

> 基于 v1.6.0（开机启动 / 下载进度 / 国内镜像 / 托盘三色角标 / 面板日志）整理的可落地方向。  
> **P1 已全部在 v1.7.0 落地**（进程自愈守护 / 日志轮转 / 操作审计 / 配置安全扫描 / 离线包导入 / 掉线告警），见下方 ✅ 标注。  
> **P2 已全部在 v1.8.0 落地**（流量与资源监控 / 表单式代理编辑器 / 配置模板库 / 开机启动增强 / 中英文切换），见下方 ✅ 标注。  
> **P3 已全部在 v1.9.0 落地**（配置快照与回滚 / frp token·加密向导 / frp 升级至 v0.71.0），见下方 ✅ 标注。  
> **v1.10.0 / v1.11.0 为交互修复与打磨版**（非路线图项）：v1.10.0 修复代理编辑器完全不工作、操作审计独立成页、下拉框与双主题 UI 统一、按页懒加载提速；**v1.11.0 按职能重新拆分页面（新增「系统设置」「安全中心」）、修复自愈守护区块间距、并修掉状态接口重复全进程扫描导致打开网页慢的问题**。  
> **v1.13.0 / v1.13.1 / v1.13.2 为双语与打磨版**（非路线图项）：v1.13.0 打赏码 / 更新日志 / 中英双语 README / 定时重启 FRP；v1.13.1 修复语言按钮未绑事件、启动恢复语言偏好；**v1.13.2 打通登录页 i18n（白名单 + 前缀字典修复）、多行文本与动态内容兜底翻译、后端文案双语化、切语言顺序与服务端语言缓存失效**。  
> 优先级：🔴 高（用户痛点强、改动可控）｜🟡 中（体验/竞争力）｜🟢 低（锦上添花）。  
> 体量：S（<1天）｜M（1-3天）｜L（>3天）。

---

## 一、稳定性与可观测性（🔴 优先）

1. **进程自愈守护** — S/M ✅（v1.7.0 已完成）
   - frpc/frps 异常退出后自动重启（带崩溃计数与退出码记录），避免“半夜悄悄掉线”。
   - 连续崩溃 N 次后停止自启并告警，防止死循环。
   - 复用现有 `get_frp_status()` + 定时 `pollStatus()`，新增 watchdog 线程。
2. **流量与资源监控** — M
   - 对接 frps 管理 API 的 `GET /api/proxy/tcp` 等，展示每个代理的今日入/出流量、当前连接数。
   - 进程级 CPU/内存占用（psutil 已有依赖）。
   - 仪表盘首页新增「流量排行」卡片。
3. **日志轮转与归档** — S ✅（v1.7.0 已完成）
   - 按大小（如 5MB）/ 时间切割 `logs/frp_*.log`，保留最近 N 份，避免盘子被写满。
   - 面板日志增加「下载/导出」按钮。
4. **多实例隔离** — M
   - 同一机器跑多套 frpc（不同配置文件/不同 server）时，能分别命名、分别启停、分别看状态。

---

## 二、配置能力升级（🔴 高价值）

1. **表单式代理编辑器** — L
   - 当前是纯文本框。改为解析 `frpc.toml` 的 `[[proxies]]` 结构，提`供“增/删/改代理条目”的表单
     （类型、本地地址、远程端口、域名、鉴权），保存时再序列化为 TOML。`
   - `降低新手门槛，直接对标商业内网穿透面板。`
2. **配置模板库** — M
   - 内置常见场景模板：SSH 远程、Web 站点（http/https + 泛域名）、游戏联机、群晖/威联通、远程桌面。
   - 一键套用 + 占位符替换（本机 IP、域名）。
3. **配置快照与回滚** — M
   - 每次保存生成快照，可对比差异、一键回退到任意历史版本。
4. **frp token / 加密向导** — M ✅（v1.9.0 已完成）
   - 引导生成 `token`，以及 stcp / sudp（需双方口令）等更安全的穿透方式配置。

---

## 三、下载 / 更新 / 部署（🟡）

1. **下载校验与断点续传** — M
   - 下载后校验官方 release 的 sha256；网络中断支持续传，避免大文件重头再来。
2. **面板自更新（self-update）** — L
   - 一键升级整个程序：拉取新版本 `index.html` / `main.py` / 依赖，自动备份旧版。
   - 与现有 `app_settings.ini` / `configs` 分离，更新不丢配置。
3. **离线包导入** — S ✅（v1.7.0 已完成）
   - 支持直接把已下载的 `frp_*.zip` 拖进界面完成安装，免去联网环境受限时的麻烦。
4. **安装向导 / 打包分发** — L
   - Windows：InnoSetup / NSIS 打包 exe，安装时可选“注册开机启动”“创建桌面快捷方式”。
   - Linux：DEB/RPM 或 Docker 镜像（目前只有 systemd 片段）。
   - macOS：dmg 打包 + 公证。
5. **开机启动增强** — M
   - 支持“延迟启动”（等网络就绪再拉起）、“以系统/服务身份运行”（Windows 任务计划、Linux 系统级 systemd）。
   - 开机启动后若 frp 未就绪，自动延迟重试。

---

## 四、安全与审计（🟡）

1. **操作审计日志** — S ✅（v1.7.0 已完成）
   - 记录谁（会话）、何时、做了什么启停/改配置操作，写入独立审计日志。
2. **登录加固** — S
   - 登录失败限流、IP 白名单、可选 TOTP 二次验证。
3. **frp 配置安全扫描** — S ✅（v1.7.0 已完成）
   - 启动时检查：是否暴露了 `admin` 面板且无密码、token 是否为空等风险项并提示。

---

## 五、多端 / 协作 / 告警（🟢）

1. **掉线告警推送** — M ✅（v1.7.0 已完成）
   - frp 失联时推送到 微信/钉钉/飞书/邮件（webhook 配置在面板里填）。
2. **移动端 PWA** — M
   - 把现有响应式页面加 manifest + service worker，可“添加到主屏幕”、离线看状态。
3. **多节点集中管理** — L
   - 一个面板管多台 frp 服务器（远端 agent 上报状态），适合有多台服务器/多个 frps 的用户。

---

## 六、国际化与体验（🟢）

1. **中英文切换** — M（已有暗色/亮色主题，可复用同一套机制）。
2. **CI 自动化构建与发布** — M
   - GitHub Actions 打出 Windows/Linux/macOS 三端包 + 自动更新 README 下载链接。
3. **可访问性 / 键盘快捷键** — S。

---

## 建议落地顺序（按 ROI）

1. **P1（已在 v1.7.0 完成 ✅）**：① 进程自愈守护、③ 日志轮转、⑭ 操作审计、⑯ 配置安全扫描、⑪ 离线包导入、⑰ 掉线告警。  
   —— 都是小改动、用户痛点明确，且都在现有架构内。
2. **P2（已在 v1.8.0 完成 ✅）**：② 流量与资源监控、⑤ 表单式代理编辑器、⑥ 配置模板库、⑬ 开机启动增强、⑳ 中英文切换。  
   —— 形成“新手也能用”的产品壁垒。
3. **P3（平台化）**：⑩ 面板自更新、⑫ 安装向导/打包、⑲ 多节点集中管理、⑱ PWA。  
   —— 决定项目能否从“个人工具”走向“可分发产品”。

---

## 后续建议（P3 落地清单与优先级）

P1 / P2 已收口（v1.7.0 / v1.8.0）。下面按「先补配置闭环 → 再做分发 → 最后平台化」的顺序整理剩余项，  
全部可在现有单文件前端 + Flask 架构内增量实现，无需重写。

### A. 配置闭环（**已在 v1.9.0 交付 ✅**）

- **⑦ 配置快照与回滚**（M ✅）：每次保存自动生成快照（复制为 `configs/snapshots/<type>/<时间戳>.cfg` + `index.json`，保留最近 50 份），前端可查看/对比/一键回滚/删除，「保存时自动快照」可在设置中开关。
- **⑧ frp token / 加密向导**（M ✅）：一键生成密码学安全 token 并写入 frps 与 frpc 的 `[auth]` 段（兼容 TOML/INI），并提供 STCP/SUDP 预共享密钥代理快速添加；frp 内置版本同步升级至 v0.71.0（修复 CVE-2026-40910）。

### B. 下载与分发（决定可分发度）

- **⑨ 下载校验与断点续传**（M）：下载后校验官方 release 的 sha256；网络中断支持续传。
- **⑫ 安装向导 / 打包分发**（L）：Windows InnoSetup/NSIS、Linux DEB/RPM 或 Docker、macOS dmg；安装时可选“注册开机启动”“创建桌面快捷方式”。
- **㉑ CI 自动化构建与发布**（M）：GitHub Actions 打三端包 + 自动更新 README 下载链接（与 ⑫ 配套收益最大）。

### C. 平台化（走向产品）

- **⑩ 面板自更新**（L）：一键升级 `index.html` / `main.py` / 依赖，自动备份旧版，与 `app_settings.ini` / `configs` 分离。
- **⑱ 移动端 PWA**（M）：现有响应式页加 manifest + service worker，可“添加到主屏幕”、离线看状态。
- **⑲ 多节点集中管理**（L）：一个面板管多台 frps（远端 agent 上报状态），适合多服务器用户。

### D. 体验收尾

- **㉒ 可访问性 / 键盘快捷键**（S）：低成本打磨，建议随任意迭代顺手做。

### 推荐落地节奏

1. **v1.9.0（已在 v1.9.0 完成 ✅）**：⑦ + ⑧（配置闭环，巩固 P2 编辑能力），并把内置 frp 版本由有 CVE 的 0.67.0 升级到 0.71.0。
2. **v1.10.0（已完成 ✅ · 打磨版）**：修复 P2/P3 遗留的交互缺陷（代理编辑器未接线、审计页拖慢设置页、下拉框无箭头、启动请求过多），并统一双主题视觉。
3. **v1.11.0（已完成 ✅ · 板块重组版）**：页面按职能拆分（系统设置 / 安全中心），修复自愈守护间距，状态接口去重进程扫描（打开网页慢的主因），版本号改为后端注入。
4. **v1.12.0（已完成 ✅ · 控件专项）**：版本切换改为自定义版本选择器、客户端/服务端改为分段切换器、模板套用按钮失效修复并改为内联占位符表单、全局下拉框基底统一、侧栏「配置与下载」前移。
5. **v1.13.0（已完成 ✅ · 运维专项 + 文档）**：新增「定时重启 FRP」（按间隔小时 / 每天固定时刻重启 frpc / frps，`[restart]` 设置段 + `/api/settings/autorestart`，复用监控线程）；README 双语化（新增 `README_EN.md`）并在感谢区新增打赏二维码。
6. **v2.0.0**：⑫ + ㉑ + ⑩（打包与自更新，具备分发能力）。
7. **v2.x**：⑱ PWA → ⑲ 多节点；㉒ 穿插。

### v1.10.0 修复清单（已交付，备查）

| 问题            | 根因                                                | 修复                            |
| ------------- | ------------------------------------------------- | ----------------------------- |
| 代理编辑器点了没反应    | 新增/保存/取消按钮无事件绑定；表格行内按钮无事件委托；`loadProxies()` 从未被调用 | 补事件绑定 + 事件委托 + 进页懒加载          |
| 代理列表列错位       | 表头 6 列，`renderProxies()` 只输出 5 个 `<td>`           | 按 6 列对齐渲染，类型徽标 + 附加字段提示       |
| 操作审计页面卡顿、内容空白 | 审计块与十几个设置区块同页，且渲染方式为整块文本                          | 独立成页 + 结构化行渲染 + 关键字/成败筛选 + 导出 |
| 下拉框「不好看」      | 全局 `select{appearance:none}` 去掉了原生箭头却没补自定义箭头      | 补主题自适应 SVG 箭头并统一控件样式          |
| 启动慢 / 请求多     | 构造函数一次性发起 8+ 个请求                                  | 按页懒加载 + 轮询可见性判断               |
| 侧栏版本号过期       | 写死 `v1.2`                                         | 与程序版本同步                       |

### v1.12.0 修复清单（已交付，备查）

| 问题                    | 根因                                                        | 修复                                                      |
| --------------------- | --------------------------------------------------------- | ------------------------------------------------------- |
| 版本切换下拉「生硬」            | 原生 `<select>` 只能塞一行文字，放不下来源 / 体积 / 待执行状态；且选完还得自己点按钮       | 改为自定义版本选择器 + 待切换任务自动计算 + 实时提示 + 二次确认                    |
| 快照下拉「不自然」             | 客户端 / 服务端只有两项，却用了下拉                                       | 改为分段切换器（底层保留 `<select>`，`.value` / `change` 语义不变）       |
| 模板「套用」按钮点了没反应         | 按钮动态渲染，但列表**完全没有事件委托**                                    | 委托到 `templatesBody`；占位符改为卡片内联表单（去掉连弹 prompt）            |
| 各工具条下拉忽大忽小            | 下拉基底样式只写在 `.form-group select` 内，工具条里的没有边框 / 圆角 / 配色      | 基底提到全局 `select`，统一 hover / focus / disabled / option 配色 |
| `.form-group` 内下拉箭头压字 | `.form-group select` 的 `padding` 覆盖了箭头所需的 `padding-right` | 下拉不再参与该 padding 规则，只覆盖字号与上下内边距                          |

### v1.11.0 修复清单（已交付，备查）

| 问题                | 根因                                                                                    | 修复                                                                  |
| ----------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 一页塞 9 个模块、加载沉重    | 「配置与下载」混装了版本/安全/系统设置                                                                  | 拆出「系统设置」（frp 版本控制 / Web 设置 / 守护与告警）与「安全中心」（安全向导 / 配置安全扫描），配置页只留配置相关 |
| 「frp 二进制」命名与定位不清  | 功能实为版本管理                                                                              | 改名为 **frp 版本控制**                                                    |
| 进程自愈守护与端口映射之间没有间距 | 该卡片非标准 `.section`，只有 `margin-top` 没有 `margin-bottom`                                  | 改为标准区块 `#sec-watchdog`，统一 18px 间距并纳入导航                              |
| 打开网页慢             | `get_watchdog_status()` 在循环里调 `get_frp_status()`，单次 `/api/status` 触发 6 次全进程 psutil 扫描 | 单次扫描 + 1s TTL 缓存，启停时作废缓存；实测 `/api/status` 约 1s → 5.75ms             |
| 版本号还要手改页面         | 页面写死                                                                                  | 后端 `{{ app_version }}` 注入，跟随 `main.py VERSION`                      |
